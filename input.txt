PROMPT CREATING TABLE APPLICATION
CREATE table application (
APPLICATION_ID  VARCHAR2(20) NOT NULL,
ACTIVE_IND  VARCHAR2(1),
APPLICATION_TYPE  VARCHAR2(20),
CONTRACT_ID  VARCHAR2(20),
CURRENT_STATUS  VARCHAR2(20),
DECISION  VARCHAR2(20),
DECISION_DATE DATE,
EFFECTIVE_DATE DATE,
EXPIRY_DATE DATE,
EXTENSION_ID  NUMBER(20),
FEES_DESC  NUMBER,
FEES_PAID_IND  NUMBER(10,0),
PPDM_GUID  NUMBER(10,2),
PREVIOUS_APPLICATION_ID  VARCHAR2(20),
RATE_SCHEDULE_ID  VARCHAR2(20),
RECEIVED_DATE DATE,
REFERENCE_NUM  VARCHAR2(40),
REMARK  VARCHAR2(2000),
RESUBMISSION_IND  VARCHAR2(1),
SECTION_OF_ACT  VARCHAR2(240),
SECTION_OF_ACT_DATE DATE,
SOURCE  VARCHAR2(20),
SUBMISSION_COMPLETE_IND  VARCHAR2(1),
SUBMISSION_DESC  VARCHAR2(240),
SUBMITTED_BY  VARCHAR2(40),
SUBMITTED_DATE DATE,
SUBMITTED_TO  VARCHAR2(40),
ROW_CHANGED_BY  VARCHAR2(30),
ROW_CHANGED_DATE DATE,
ROW_CREATED_BY  VARCHAR2(30),
ROW_CREATED_DATE DATE,
ROW_QUALITY  VARCHAR2(20));

PROMPT CREATING FOREIGN KEY CONSTRAINT APP_CEX_FK
ALTER TABLE APPLICATION
 ADD (CONSTRAINT APP_CEX_FK
FOREIGN KEY(
CONTRACT_ID,
EXTENSION_ID
)REFERENCES CONT_EXTENSION (
CONTRACT_ID,
EXTENSION_ID
));
PROMPT CREATING PRIMARY KEY APP_PK
ALTER TABLE APPLICATION ADD (CONSTRAINT APP_PK PRIMARY KEY (
APPLICATION_ID));
